@model IEnumerable<string>

@{
    ViewData["Title"] = "List";
}

<h1>List</h1>

@foreach (var item in Model)
{
    <p>@item</p>
    <a asp-action="FileInfo" asp-route-name="@item">@item</a>
    <br />
}